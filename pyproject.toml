[project]
name = "termucoder-ai"
version = "0.5.0"
description = "Local AI coding assistant for Termux"
requires-python = ">=3.11"

[project.scripts]
termucoder = "termucoder.cli:main"

[tool.setuptools.packages.find]
include = ["termucoder*"]
